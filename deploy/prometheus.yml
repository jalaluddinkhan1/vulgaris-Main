global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "vulgaris"
    static_configs:
      - targets: ["vulgaris:8000"]
    metrics_path: "/metrics"
    scheme: http
